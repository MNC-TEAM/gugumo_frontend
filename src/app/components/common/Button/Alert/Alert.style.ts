import styled from "styled-components";

export const Button = styled.button`
    font-size: var(--buttonSemiboldSize);
    font-weight: var(--buttonSemiboldWeight);
    color: var(--OnError);
    background: var(--Error);
    font-family: "Pretendard";
    padding: 6px 16px;
    border-radius: 4px;
    cursor: pointer;
`;
import { Layout } from "@/app/components/main/style";
import styled from "styled-components";

export const BookMakrLayout = styled(Layout)`
    margin-top: 46px;

    @media screen and (max-width:820px) {
        margin-top: 20px;
    }

`;